1. To login:
	curl -v -X POST "http://localhost:4200/login?username=<user>&password=<pass>"
2. To send files:
	curl -v -X POST -H "Cookie: cookie=<token>" -d "data" "http://localhost:4200/<user>/path/to/file"
3. To get files:
	curl -v -H "Cookie: cookie=<token>" "http://localhost:4200/<user>/path/to/file"
